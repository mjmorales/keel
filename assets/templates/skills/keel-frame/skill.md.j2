{# keel-frame — session governance skill. Generated by Keel. #}
---
name: keel-frame
description: Session governance for {{ project_name }}. Loads architectural contracts from CLAUDE.md, enforces segment boundaries, patterns, and vocabulary. Always active. Triggers on any code modification task.
---

# Keel Frame — {{ project_name }}

You are working in a Keel-governed codebase. Read and enforce CLAUDE.md at the project root before making any changes.

## Session Start

1. Read `CLAUDE.md` at the project root.
2. Read `references/contracts.json` in this skill directory for machine-readable contracts.
3. You now know the segments, constraints, patterns, forbidden imports, and vocabulary.

## Rules

### Segment Boundaries

Every file belongs to exactly one segment. Before creating or modifying a file, determine its segment from the path:

{% for seg in segments -%}
- `{{ seg.path }}` → **{{ seg.name }}** [{{ seg.constraints | join(', ') }}] ({{ seg.language }})
{% endfor %}

Do not create files outside declared segments. Do not import across segment boundaries except through schema types or caller-defined interfaces.

### Patterns Are Mandatory

Each segment has assigned patterns. Use them. Do not substitute alternatives.

{% for seg in segments -%}
**{{ seg.name }}**:
{% for p in seg.patterns -%}
- {{ p.name }}: {{ p.purpose }}
{% endfor %}
{% endfor %}

### Vocabulary Is Frozen

These are the only domain terms. New terms require the friction protocol.

{% for v in vocabulary -%}
- **{{ v.term }}** ({{ v.kind }}) — {{ v.definition }}
{% endfor %}

### One Way To Do Each Thing (A5)

If a pattern is assigned, every instance of that operation uses that pattern. No switches where a Registry is assigned. No callbacks where a Strategy is assigned. No ad-hoc file layouts where a template exists.

### Friction Protocol

If you cannot implement the current task without violating a contract:

1. STOP. Do not bend the architecture.
2. Write a friction report identifying the contract, the conflict, and proposed resolutions.
3. Wait for human approval before proceeding.

See FRAMEWORK.md Section 6 for the full protocol.

## File Generation

When creating new files in a segment, use the `/keel-new` command or follow the file templates declared in CLAUDE.md exactly. Do not create files with ad-hoc structure.

## Error Handling

{% for language, rules in error_strategies.items() -%}
**{{ language }}**:
{% for rule in rules -%}
- {{ rule }}
{% endfor %}
{% endfor %}
