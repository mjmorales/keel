---
description: Run contract checks against the working tree. Reports forbidden imports, I/O violations, and cross-segment imports.
argument-hint: "[--staged]"
---

# Contract Check: $ARGUMENTS

```bash
keel check $ARGUMENTS
```

If violations are found, present them and suggest fixes. For a full drift audit, use `keel audit`.
